import Card from '@mui/material/Card';
import styled from 'styled-components';

const MuiCard = styled(Card)`
    max-width: 345px;
    margin: 1rem auto;
    padding: 10px;
    display: flex;
    flex-direction: row;
`;

export {MuiCard};